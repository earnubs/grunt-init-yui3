{
    "name": "{%= name %}",
    "builds": {
        "{%= name %}": {
            "jsfiles": [
                "./js/{%= name %}.js"
            ],
        "replace": {
            "@VERSION@": "{%= version %}"
        },
            "config": {
                "requires": [
                ]
            }
        }
    }
}

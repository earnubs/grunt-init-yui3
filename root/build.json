{
    "name": "{%= name %}",
    "builds": {
        "one-{%= name %}": {
            "jsfiles": [
                "./js/{%= name %}.js"
            ],
        "replace": {
            "@VERSION@": "{%= version %}"
        },
            "config": {
                "use": [
                ]
            }
        }
    }
}

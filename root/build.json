{
    "name": "{%= name %}",
    "builds": {
        "one-{%= name %}": {
            "jsfiles": [
                "./js/{%= name %}.js"
            ],
            "config": {
                "use": [
                    {%= requires %}
                ]
            }
        }
    }
}
